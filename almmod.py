from __future__ import division
import numpy as np

def ind2lm:

def lm2ind:

class Alm_data:
    #Will store alms for a given mode (I, Q, U typically) into a numpy array
    #with one dimension to improve performance.
    def __init__(self, alms=None):
        self.alms = alms
